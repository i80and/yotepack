name: Rust

on:
  push:
    branches: [ "main", "vibecode" ]
  pull_request:
    branches: [ "main", "vibecode" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Check
      run: cargo check --verbose
    - name: Run tests
      run: cargo test --verbose
